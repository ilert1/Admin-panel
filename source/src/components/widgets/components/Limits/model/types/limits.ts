export interface UpdateLimitsType {
    payInMin: string;
    payInMax: string;
    payOutMin: string;
    payOutMax: string;
    rewardMin: string;
    rewardMax: string;
}

export type ResourceType = "direction" | "terminal";
